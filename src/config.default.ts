import type { AppConfig } from '#/app'

export const defaultAppConfig: AppConfig = {
  theme: {
    darkMode: 'auto',
  },
  layout: {
    mode: 'side',
    siderCollapsed: false,
  },
  locale: 'zh-CN',
}
